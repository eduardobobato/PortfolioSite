(function () {
    'use strict';

    angular
        .module('myApp')
        .factory('UsuarioService', [
            '$http', 'appSettings',
            function ($http, appSettings) {

                return {

                };
            }
        ]);

})();